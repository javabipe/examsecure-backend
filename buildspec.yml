version: 0.2

phases:
  pre_build:
    commands:
      - echo Installing source NPM dependencies...
      - yarn install --ignore-engines
  build:
    commands:
      - echo Build started on `date`
      - npm run build
  post_build:
      - aws s3 cp --recursive --acl public-read ./build s3://javabipe/
artifacts:
  files:
    - '**/*'
  base-directory: build
